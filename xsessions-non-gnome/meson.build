install_data(
    'pantheon-non-gnome.desktop',
    install_dir: join_paths(datadir, 'xsessions')
)
install_data(
    'startpantheon',
    install_dir: bindir
)
install_data(
    'session-manager.py',
    install_dir: bindir,
    rename: 'pantheon-session-manager'
)
